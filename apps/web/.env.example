# HubSpot CRM Contacts API (footer newsletter signup)
# Private-app access token with the `crm.objects.contacts.write` scope.
# Create one at: HubSpot → Settings → Integrations → Private Apps → Create a private app
# This is a SECRET — never commit the real value or expose it to the client.
HUBSPOT_ACCESS_TOKEN=
